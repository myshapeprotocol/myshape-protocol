# We Built an Engine That Detects AI-Generated Human Motion. GPT-5 and DeepSeek Both Failed.

**MyShape Protocol · June 2026**

---

AI can generate a face. AI can clone a voice. AI can forge a fingerprint.

But AI cannot generate *you* — not your specific, irreducibly biological, physically-constrained human motion. And we have the numbers to prove it.

## The Experiment

We built a Rust-based verification engine that analyzes human motion through four independent feature dimensions. Then we ran a simple test:

1. **Enroll a human**: 20 motion samples → one cryptographic signature.
2. **Issue a challenge**: "Draw a circle with your right hand. Tilt your torso 12 degrees. Keep your head still." — unpredictable, multi-joint, with a coupling constraint that shares a kinetic chain.
3. **Test three responses**:
   - **Genuine human** repeating the challenge
   - **AI-generated forgery** — same trajectory, perfect geometry, zero tremor
   - **Different human** (impostor)

## The Results

```
Test Case              Presence Score   Verdict
─────────────────────────────────────────────────
Genuine Human          0.9817           PASS ✓
AI Forgery              0.5857           FAIL ✗
Impostor                0.0000           FAIL ✗

Human—AI Gap: 0.3960
```

The engine rejected the AI-generated motion across four independent dimensions:

| Rejection Tag | What Failed |
|:---|:---|
| `TREMOR_ABSENT` | 8–12 Hz physiological tremor missing — AI has no stretch reflex arc |
| `JERK_SPECTRUM_ANOMALY` | 1/f spectral scaling violated — AI jerk is either over-smoothed or white noise |
| `HURST_ANOMALY` | Long-range dependence absent — AI acceleration is uncorrelated noise |
| `OVER_SMOOTHED` | Muscle micro-perturbations missing — too perfect to be biological |

## Why This Matters

Every identity system in production today — passwords, biometrics, hardware wallets, KYC — answers one question: "Does the credential match?"

None of them answer: "Is the human who enrolled that credential physically present right now?"

This gap has existed for decades. AI makes it fatal. 

In a world where AI agents hold private keys, where deepfakes pass face recognition, where GPT-5 can generate convincing video of anyone doing anything — the only signal that cannot be forged is the real-time, physics-bound motion of a living human body.

Motion is not a file. Motion is not a template. Motion is a continuous, high-dimensional, noise-driven field generated by the irreducible physics of your nervous system and your skeleton. AI can approximate its output. It cannot replicate its entropy.

## How It Works

The engine extracts a 128-dimensional signature from four feature groups:

- **Kinematics (40 dims)**: Skeletal ratios between 14 bone segments. Your femur-to-tibia ratio is physically unique. AI doesn't know your bone lengths.
- **Acceleration Profile (25 dims)**: Statistical distribution including Hurst exponent. Human acceleration: H ≈ 0.6–0.8 (long-range dependence). AI acceleration: H ≈ 0.5 (uncorrelated).
- **Jerk Profile (25 dims)**: The third derivative of position. Rate of change of acceleration. The most unforgeable single kinematic dimension.
- **Jerk Spectrum (30 dims)**: Frequency-domain analysis. Human jerk follows 1/f^α scaling (α ≈ 1.0–1.5) reflecting multi-scale biological motor control. AI jerk: α > 2.0 (over-smoothed) or α ≈ 0 (white noise).

The composite score combines motion signature, device attestation, and context verification. Hard gates reject on rooted devices, impossible location jumps, or sensor inconsistency.

All processing happens on-device. Only a zero-knowledge proof leaves the device. No raw motion data is stored or transmitted.

## Run It Yourself

The core engine is open source:

```bash
git clone https://github.com/myshapeprotocol
cd cli
cargo run --release --bin myshape-demo -- --verbose
```

25 tests. Zero dependencies beyond the Rust standard library and audited crypto crates. Compiles to native binaries and WebAssembly.

Or see the live dashboard at [myshape.com/developers](https://www.myshape.com/developers).

## The Deeper Truth

Every AI motion model — diffusion, transformer, VAE — is trained with L2 loss. L2 loss penalizes the square of the error. A 1 mm tremor deviation is penalized 100× less than a 10 mm trajectory error. The model learns to suppress high-frequency, low-amplitude signals — exactly the signals that make human motion human.

This is not a temporary AI limitation. It is a structural property of neural network training. The optimization objective (minimize prediction error) is fundamentally at war with the measurement objective (detect the absence of biological noise). The better AI gets at generating realistic motion, the more aggressively it smooths — and the more detectable it becomes.

**The AI Paradox**: Every improvement in visual fidelity comes at the cost of spectral fidelity. AI faces an impossible tradeoff: look more real, or be more real. It cannot do both.

---

## What We're Building

MyShape is a presence verification protocol. Not proof of *identity* — proof of *presence*.

World (the orb project) proves you're *a* human. MyShape proves you're *this* human — physically present, authorizing this specific operation.

We're currently in closed development. If you're building in the identity, security, agent infrastructure, or applied cryptography space, we'd like to talk.

**Read the whitepaper**: [myshape.com/whitepaper](https://www.myshape.com/whitepaper)  
**Contact**: [protocol@myshape.com](mailto:protocol@myshape.com)  
**Follow**: [@myshapeprotocol](https://x.com/myshapeprotocol)

---

*MyShape Protocol — The Sovereign Presence Layer for the AI Era.*
